services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-dashboard
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
